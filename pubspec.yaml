name: smart_class_checkin_app
description: Smart Class Check-in and Learning Reflection App
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.3+1
  path: ^1.9.0
  uuid: ^4.5.1
  geolocator: ^13.0.2
  geocoding: ^3.0.0
  http: ^1.2.2
  mobile_scanner: ^6.0.2
  permission_handler: ^11.3.1
  intl: ^0.19.0
  firebase_core: ^3.10.0
  cloud_firestore: ^5.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
